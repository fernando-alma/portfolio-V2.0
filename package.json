{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo of Fernando Alma's Portfolio-V3.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter client build",
    "start": "pnpm --filter api start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
